apiVersion: v1
kind: Service
metadata:
  name: mymavenapp
  labels:
    app: mymavenapp
spec:
  type: NodePort  
  ports:
    - port: 8081  
      targetPort: 8081  
      protocol: TCP
  selector:
    app: mymavenapp
